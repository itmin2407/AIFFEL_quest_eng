"""
config.py — 라벨, 이미지, 모델 설정을 중앙에서 관리합니다.

📌 이미지 추가 방법:
  images/ 폴더 안에 라벨명과 동일한 이름의 파일을 넣으세요.
  파일명이 곧 정답 라벨이 됩니다.

  images/
  ├── 고양이.jpg     ← CLIP이 "고양이"로 맞혀야 하는 이미지
  ├── 강아지.png     ← CLIP이 "강아지"로 맞혀야 하는 이미지
  ├── 피자.jpg
  └── 해변.jpg

📌 라벨 추가/삭제 방법:
  - LABEL_SETS 딕셔너리에 새 세트를 추가하거나 기존 세트의 라벨을 수정하세요.
  - images/ 폴더에 대응하는 이미지 파일을 함께 추가하세요.
  - 서버를 재시작하면 자동으로 반영됩니다.
"""
from pathlib import Path

# ── 경로 설정 ──────────────────────────────────────────────────────────────
BASE_DIR = Path(__file__).resolve().parent.parent
IMAGES_DIR = BASE_DIR / "images"

# 허용 이미지 확장자
ALLOWED_EXTENSIONS = {".jpg", ".jpeg", ".png", ".webp", ".gif"}

# ── 모델 설정 ──────────────────────────────────────────────────────────────
MODEL_NAME = "openai/clip-vit-base-patch32"

# ── 라벨 세트 정의 ──────────────────────────────────────────────────────────
# 각 세트에 자유롭게 라벨을 추가/삭제/수정하세요.
LABEL_SETS: dict[str, list[str]] = {
    # "일반": [
    #     "사람",
    #     "자동차",
    #     "건물",
    #     "음식",
    #     "동물",
    #     "자연",
    #     "스포츠",
    #     "기술/전자기기",
    # ],
    "동물": [
        "고양이",
        "강아지",
        # "새",
        # "물고기",
        # "토끼",
        # "곰",
        # "사자",
        # "코끼리",
        "늑대",
    ],
    # "음식": [
    #     "피자",
    #     "햄버거",
    #     "초밥",
    #     "파스타",
    #     "샐러드",
    #     "케이크",
    #     "라면",
    #     "치킨",
    # ],
    # "장소": [
    #     "해변",
    #     "산",
    #     "도시",
    #     "숲",
    #     "사막",
    #     "공원",
    #     "실내",
    #     "농장",
    # ],
    # "교통수단": [
    #     "자동차",
    #     "버스",
    #     "기차",
    #     "비행기",
    #     "배",
    #     "오토바이",
    #     "자전거",
    #     "트럭",
    # ],
}

# ── 기본 라벨 세트 ──────────────────────────────────────────────────────────
DEFAULT_LABEL_SET = "일반"

# ── 추론 설정 ──────────────────────────────────────────────────────────────
TOP_K_DEFAULT = 3
TOP_K_MAX = 10
